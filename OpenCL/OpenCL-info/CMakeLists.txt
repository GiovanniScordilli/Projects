cmake_minimum_required(VERSION 3.16)
project(OCLInfo LANGUAGES C)
file(GLOB src * .c .h)
find_package(OpenCL 3.0 REQUIRED)
add_executable(OCLInfo ${src})
target_link_libraries(OCLInfo OpenCL::OpenCL)
